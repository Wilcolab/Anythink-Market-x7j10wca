/**
 * Converts a string to camelCase format.
 * 
 * This function takes a string with words separated by spaces, hyphens, 
 * underscores, or other non-alphanumeric characters and converts it to 
 * camelCase where the first word is lowercase and subsequent words have 
 * their first letter capitalized.
 * 
 * @param {string} str - The input string to convert
 * @returns {string} The camelCase version of the input string
 * 
 * @example
 * // Convert space-separated words
 * toCamelCase("hello world"); // returns "helloWorld"
 * 
 * @example
 * // Convert hyphen-separated words
 * toCamelCase("my-variable-name"); // returns "myVariableName"
 * 
 * @example
 * // Convert underscore-separated words
 * toCamelCase("user_first_name"); // returns "userFirstName"
 */
function toCamelCase(str) {
    // Implementation goes here
}